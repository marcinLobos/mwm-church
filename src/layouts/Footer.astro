---
// Tu zaczyna się sekcja logiki
import StyledText from "@/components/StyledText";
import SocialMediaLinks from "@/components/SocialMediaLinks";
import { siteConfig } from "@/data/contentCollectionDataFetching";

const left = siteConfig?.data.footer?.left;
// Tu kończy się sekcja logiki - te kreski poniżej są KLUCZOWE
---

<footer class="bg-slate-900 text-white layout-base py-12 border-t border-white/10">
  <div class="grid gap-8 md:grid-cols-2">
    <section class="flex flex-col gap-6">
      <h2 class="text-2xl font-bold uppercase tracking-tighter">MWM Architekci</h2>
      <div class="flex flex-col gap-4 text-gray-400">
        <p>ul. św. Michała Archanioła 7<br/>35-310 Rzeszów</p>
        <p>T: +48 17 853 90 71<br/>E: biuro@mwmarchitekci.pl</p>
      </div>
      
      {
        left?.social && left.social.length > 0 && (
          <div class="pt-4 grayscale opacity-50 hover:opacity-100 transition-opacity">
            <SocialMediaLinks social={left.social} client:idle />
          </div>
        )
      }
    </section>

    <section class="flex flex-col justify-end md:items-end text-right">
       <div class="max-w-xs border-r-2 border-white/20 pr-4">
          <p class="text-[10px] text-gray-500 font-mono leading-tight">
            // ENGINE: ASTRO_3_FLASH <br/>
            // DEPLOY: VERCEL_STATION <br/>
            // AUTH: MIKA_PROTOTYPE
          </p>
          <p class="mt-4 italic text-sm text-gray-400 font-serif">
            "Architektura to porządek w chaosie wszechświata."
          </p>
       </div>
    </section>
  </div>
</footer>